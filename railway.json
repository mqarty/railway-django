{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "python manage.py collectstatic --noinput --clear && python manage.py migrate && gunicorn backend.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 1
    }
}